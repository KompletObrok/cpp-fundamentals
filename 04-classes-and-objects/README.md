# 04 - Movie Collection

Class composition exercise: `Movie` entity class managed by a `Movies` collection class using `std::vector`. Covers const-correctness, `std::move` semantics, and range-based for loops.

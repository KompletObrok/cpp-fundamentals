# 06 - Inheritance

Account class hierarchy exercise: `Account` base class with `Savings_Account`, `Checking_Account`, and `Trust_Account` derived classes. Covers public inheritance, method overriding, delegating to base class methods, and `static constexpr` member defaults.
